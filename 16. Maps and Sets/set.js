// A Set is a special type collection – “set of values” (without keys), where each value may occur only once.

let set = new Set();

set.add('John');
set.add(3);
set.add('John');

console.log(set);
// It has similar loop structure as of maps
