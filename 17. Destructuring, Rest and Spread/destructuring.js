// We sometimes need just variables inside array or objects, so we can use destructuring

let arr = [1,2,3,4,5];

let [first, second] = arr; // First element will be stored in first and so on.

console.log(first, second);

